sudo: required
language: generic 
services: 
  - docker

before_install:
  - docker build -t holadepo/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true holadepo/docker-react npm run test
